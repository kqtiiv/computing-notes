module ParametricPolymorphism () where

-- a function is parametric if it does the same thing regarless of the value
-- e.g., the identity function always returns the input

-- polymorphism: a function that is overloaded but with different implementations
-- e.g, + is definied for Int and Double, bt must use values in a computational way

